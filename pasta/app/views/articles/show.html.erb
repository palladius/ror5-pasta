<h1><%= @article.title %></h1>

	<p>
	  <strong>Text:</strong> 
	  <span class='articles_text'><%= @article.text %></span>
	</p>
	<p>
	  <strong>URL:</strong><a href="<%= @article.url %>" ><%= @article.url %></a>
	</p>
	 
<h2>Comments</h2>

  <%= render @article.comments %>

 
<h2>Add a comment:</h2>

<%= render 'comments/form' %>
	 
	<%= link_to 'Edit', edit_article_path(@article) %> |
	<%= link_to 'Back', articles_path %>